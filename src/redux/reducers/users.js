const InitialState = {
  isFetching: true,
  error: '',
  isAuthed: false,
  authedId: '',
}

export default function users(state = InitialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
